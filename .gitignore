# Python / uv
.venv/
__pycache__/
*.pyc

# Scraper raw-response cache (regenerable; not part of the corpus)
data/cache/

# Model weights / adapters / compiled browser bundles (too big for git)
models/
