# Python / uv
.venv/
__pycache__/
*.pyc

# Regenerable caches: scraper raw responses + built MakeCode projects.
cache/

# Model weights / adapters / compiled browser bundles (too big for git)
models/

# Verify toolchain: npm-installed tools (regenerable).
src/microbit_tuner/verify/node_modules/
