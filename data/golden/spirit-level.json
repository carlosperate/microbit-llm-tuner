{
  "slug": "spirit-level",
  "url": "https://microbit.org/projects/make-it-code-it/spirit-level/",
  "license": "CC BY-SA 4.0",
  "programs": [
    {
      "task": "",
      "makecode_ts": {
        "pub_id": "_esA53UP5jfe1",
        "source": "basic.forever(function () {\n    if (input.rotation(Rotation.Pitch) < 5 && input.rotation(Rotation.Pitch) > -5 && (input.rotation(Rotation.Roll) < 5 && input.rotation(Rotation.Roll) > -5)) {\n        basic.showIcon(IconNames.Yes)\n        music.ringTone(523)\n    } else {\n        basic.showIcon(IconNames.No)\n        music.ringTone(0)\n    }\n})\n",
        "dependencies": {
          "core": "*",
          "radio": "*",
          "microphone": "*"
        }
      },
      "micropython": {
        "source": "from microbit import *\nimport music\n\n# Uses accelerometer readings in the x and y axis \n# and also allows the micro:bit to be slightly off-level\n# to make it work better in practice\nwhile True:\n    if accelerometer.get_x() > -10 and accelerometer.get_x() < 10 and accelerometer.get_y() > -10 and accelerometer.get_y() < 10:\n        display.show(Image.YES)\n        music.play('C5:1')\n        sleep(200)\n    else:\n        display.show(Image.NO)\n"
      }
    }
  ]
}
